# Local Presentation preview (used by `sanity dev`)
SANITY_STUDIO_PREVIEW_URL=http://localhost:5173

# For `sanity deploy`, either unset this so production Netlify is used,
# or set it explicitly:
# SANITY_STUDIO_PREVIEW_URL=https://bbart-svelte.netlify.app
