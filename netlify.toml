# Publish is relative to the repo root. Do not add a netlify.toml under web/ —
# Netlify prefers that file when the package directory is web, and would
# resolve publish = "build" as /build instead of /web/build.
[build]
	command = "pnpm run build"
	publish = "web/build"
