PUBLIC_SANITY_STUDIO_URL=http://localhost:3333
# Viewer (or higher) token — required for Presentation /preview/enable on Netlify
SANITY_VIEWER_TOKEN=
# Optional stable cookie secret for preview mode (defaults to SANITY_VIEWER_TOKEN)
SANITY_PREVIEW_SECRET=
